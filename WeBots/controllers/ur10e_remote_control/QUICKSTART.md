# Быстрый старт - Удаленное управление UR10e

## Шаг 1: Настройка мира Webots

Откройте файл мира (например, `UR10e_table.wbt`) и убедитесь, что контроллер указан:

```
UR10e {
  controller "ur10e_remote_control"
  ...
}
```

## Шаг 2: Запуск симуляции

1. Откройте мир в Webots
2. Нажмите кнопку **Play** (▶) для запуска симуляции
3. В консоли Webots вы должны увидеть:
   ```
   TCP сервер запущен на порту 10001
   HTTP сервер запущен на порту 10002
   ```

## Шаг 3: Выбор способа управления

### Вариант A: Веб-интерфейс (самый простой)

1. Откройте в браузере: **http://localhost:10002/**
2. Используйте кнопки и формы для управления роботом

### Вариант B: TCP клиент

1. Убедитесь, что симуляция запущена
2. Запустите TCP клиент:
   ```bash
   python WeBots/controllers/ur10e_remote_control/tcp_client_example.py
   ```
3. Введите команды в интерактивном режиме

### Вариант C: HTTP клиент

1. Убедитесь, что симуляция запущена
2. Запустите HTTP клиент:
   ```bash
   python WeBots/controllers/ur10e_remote_control/http_client_example.py
   ```
3. Введите команды в интерактивном режиме

### Вариант D: Командная строка

**TCP:**
```bash
python WeBots/controllers/ur10e_remote_control/tcp_client_example.py "vel 1 0.5"
```

**HTTP:**
```bash
python WeBots/controllers/ur10e_remote_control/http_client_example.py "vel 1 0.5"
```

**Или через curl:**
```bash
curl "http://localhost:10002/command?cmd=vel%201%200.5"
```

## Примеры команд

```bash
# Получить справку
help

# Получить статус
status

# Вращать сустав 1 со скоростью 0.5 рад/с
vel 1 0.5

# Установить скорости всех суставов
vel_all 0.5 0.3 0.4 0.2 0.3 0.25

# Установить позицию сустава 1 (90 градусов)
pos 1 1.57

# Вернуть в исходное положение
home

# Остановить все моторы
stop
```

## Устранение проблем

### Ошибка: "Connection refused" или "No connection could be made"

**Причина:** Сервер не запущен

**Решение:**
1. Убедитесь, что Webots запущен
2. Убедитесь, что симуляция запущена (кнопка Play)
3. Проверьте консоль Webots - должны быть сообщения о запуске серверов
4. Убедитесь, что в мире указан контроллер `ur10e_remote_control`

### Ошибка: "ModuleNotFoundError: No module named 'requests'"

**Решение:**
```bash
pip install requests
```

### Порты заняты

Если порты 10001 или 10002 заняты, измените их в файле `ur10e_remote_control.py`:
```python
TCP_PORT = 10001  # Измените на другой порт
HTTP_PORT = 10002  # Измените на другой порт
```

## Проверка работы

1. Запустите симуляцию в Webots
2. Откройте браузер: http://localhost:10002/
3. Нажмите кнопку "Status" - должны увидеть информацию о суставах
4. Нажмите кнопку "Home" - робот должен начать движение

Если все работает, вы готовы к использованию! 🎉

