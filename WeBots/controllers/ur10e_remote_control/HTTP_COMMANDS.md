# Список HTTP команд для управления UR10e

Базовый URL: `http://localhost:10002`

---

## 📋 Базовые команды

### 1. Получить справку
```
http://localhost:10002/command?cmd=help
```

### 2. Получить статус робота
```
http://localhost:10002/status
```

### 3. Остановить все моторы
```
http://localhost:10002/command?cmd=stop
```

### 4. Вернуть в исходное положение
```
http://localhost:10002/command?cmd=home
```

---

## ⚙️ Управление скоростями

### 5. Установить скорость сустава 1 (0.5 рад/с)
```
http://localhost:10002/command?cmd=vel%201%200.5
```

### 6. Установить скорость сустава 2 (0.3 рад/с)
```
http://localhost:10002/command?cmd=vel%202%200.3
```

### 7. Установить скорость сустава 3 (0.4 рад/с)
```
http://localhost:10002/command?cmd=vel%203%200.4
```

### 8. Установить скорость сустава 4 (0.2 рад/с)
```
http://localhost:10002/command?cmd=vel%204%200.2
```

### 9. Установить скорость сустава 5 (0.3 рад/с)
```
http://localhost:10002/command?cmd=vel%205%200.3
```

### 10. Установить скорость сустава 6 (0.25 рад/с)
```
http://localhost:10002/command?cmd=vel%206%200.25
```

### 11. Установить скорости всех суставов одновременно
```
http://localhost:10002/command?cmd=vel_all%200.5%200.3%200.4%200.2%200.3%200.25
```
Порядок: сустав 1, сустав 2, сустав 3, сустав 4, сустав 5, сустав 6

---

## 📍 Управление позициями

### 12. Установить позицию сустава 1 (90 градусов = 1.57 радиан)
```
http://localhost:10002/command?cmd=pos%201%201.57
```

### 13. Установить позицию сустава 2 (45 градусов = 0.785 радиан)
```
http://localhost:10002/command?cmd=pos%202%200.785
```

### 14. Установить позицию сустава 3 (180 градусов = 3.14 радиан)
```
http://localhost:10002/command?cmd=pos%203%203.14
```

### 15. Установить позиции всех суставов одновременно
```
http://localhost:10002/command?cmd=pos_all%200%200.5%200%200%200%200
```
Порядок: сустав 1, сустав 2, сустав 3, сустав 4, сустав 5, сустав 6

---

## 🔄 Примеры последовательностей

### 16. Последовательность: домой → вращение → остановка
```
Шаг 1: http://localhost:10002/command?cmd=home
Шаг 2: http://localhost:10002/command?cmd=vel%201%200.5
Шаг 3: http://localhost:10002/command?cmd=stop
```

### 17. Вращение нескольких суставов
```
http://localhost:10002/command?cmd=vel%201%200.5
http://localhost:10002/command?cmd=vel%202%200.3
http://localhost:10002/command?cmd=vel%203%200.4
```

### 18. Установка позиций нескольких суставов
```
http://localhost:10002/command?cmd=pos%201%201.57
http://localhost:10002/command?cmd=pos%202%200.785
http://localhost:10002/command?cmd=pos%203%201.57
```

---

## 📊 Таблица перевода градусов в радианы

| Градусы | Радианы | Пример команды |
|---------|---------|----------------|
| 0°      | 0       | `pos%201%200` |
| 30°     | 0.524   | `pos%201%200.524` |
| 45°     | 0.785   | `pos%201%200.785` |
| 60°     | 1.047   | `pos%201%201.047` |
| 90°     | 1.571   | `pos%201%201.571` |
| 120°    | 2.094   | `pos%201%202.094` |
| 180°    | 3.142   | `pos%201%203.142` |
| 270°    | 4.712   | `pos%201%204.712` |
| 360°    | 6.283   | `pos%201%206.283` |

---

## 🎯 Рекомендуемые скорости

| Действие | Скорость (рад/с) | Пример |
|----------|------------------|--------|
| Медленное | 0.1 - 0.3 | `vel%201%200.2` |
| Среднее | 0.3 - 0.5 | `vel%201%200.4` |
| Быстрое | 0.5 - 1.0 | `vel%201%200.7` |
| Очень быстрое | 1.0 - 2.0 | `vel%201%201.5` |

⚠️ **Внимание:** Слишком высокие скорости могут привести к нестабильному движению!

---

## 💡 Как использовать

### В браузере:
1. Откройте браузер
2. Вставьте URL в адресную строку
3. Нажмите Enter
4. Увидите JSON ответ с результатом

### С помощью cURL (командная строка):
```bash
curl "http://localhost:10002/command?cmd=vel%201%200.5"
```

### С помощью Python:
```python
import requests
response = requests.get('http://localhost:10002/command?cmd=vel%201%200.5')
print(response.json())
```

### С помощью JavaScript:
```javascript
fetch('http://localhost:10002/command?cmd=vel%201%200.5')
    .then(r => r.json())
    .then(data => console.log(data));
```

---

## 📝 Формат ответа

Все команды возвращают JSON:

**Успех:**
```json
{
  "status": "ok",
  "command": "vel",
  "message": "Сустав 1: скорость = 0.500 рад/с"
}
```

**Ошибка:**
```json
{
  "status": "error",
  "message": "Номер сустава должен быть от 1 до 6"
}
```

**Статус:**
```json
{
  "status": "ok",
  "command": "status",
  "data": {
    "motors": [
      {
        "joint": 1,
        "name": "shoulder_pan_joint",
        "mode": "velocity",
        "velocity": 0.5,
        "target_position": null
      }
    ],
    "time": 12.345
  }
}
```

---

## 🔧 Часто используемые команды

### Быстрый старт:
1. `http://localhost:10002/status` - проверить статус
2. `http://localhost:10002/command?cmd=home` - вернуть в исходное положение
3. `http://localhost:10002/command?cmd=vel%201%200.5` - начать движение
4. `http://localhost:10002/command?cmd=stop` - остановить

### Для тестирования:
- `http://localhost:10002/command?cmd=help` - справка
- `http://localhost:10002/status` - текущее состояние
- `http://localhost:10002/command?cmd=vel_all%200.5%200.3%200.4%200.2%200.3%200.25` - движение всех суставов

---

## ⚠️ Важные замечания

1. **Кодирование URL**: Пробелы в командах должны быть заменены на `%20`
   - Пример: `vel 1 0.5` → `vel%201%200.5`

2. **Номера суставов**: От 1 до 6
   - 1 = shoulder_pan_joint (поворот основания)
   - 2 = shoulder_lift_joint (подъем плеча)
   - 3 = elbow_joint (локоть)
   - 4 = wrist_1_joint (запястье 1)
   - 5 = wrist_2_joint (запястье 2)
   - 6 = wrist_3_joint (запястье 3)

3. **Единицы измерения**:
   - Скорости: **радианы в секунду** (рад/с)
   - Позиции: **радианы** (рад)

4. **Безопасность**: Всегда используйте `stop` перед изменением режима управления

---

## 🎮 Готовые последовательности команд

### Последовательность 1: Плавное движение
```
1. http://localhost:10002/command?cmd=home
2. http://localhost:10002/command?cmd=vel%201%200.2
3. http://localhost:10002/command?cmd=vel%202%200.2
4. http://localhost:10002/command?cmd=stop
```

### Последовательность 2: Движение всех суставов
```
1. http://localhost:10002/command?cmd=home
2. http://localhost:10002/command?cmd=vel_all%200.3%200.3%200.3%200.2%200.2%200.2
3. (подождать несколько секунд)
4. http://localhost:10002/command?cmd=stop
```

### Последовательность 3: Установка позиций
```
1. http://localhost:10002/command?cmd=home
2. http://localhost:10002/command?cmd=pos%201%201.57
3. http://localhost:10002/command?cmd=pos%202%200.785
4. http://localhost:10002/command?cmd=home
```

---

**Примечание:** Убедитесь, что Webots запущен и симуляция активна перед использованием этих команд!

